<div class="ui breadcrumb">
  <%= link_to 'My Courses', courses_path, class: 'section' %>
  <span class="divider">/</span>
  <div class="active section">Editing <%= @course.name %></div>
</div>

<div class="ui divider"></div>

<h1>Editing Course</h1>

<%= render 'form', course: @course %>


<h2 class="ui red header">Danger zone!</h2>

<div class="ui icon negative message">
  <i class="fire extinguisher icon"></i>
  <div class="content">
    <div class="header">
      Be careful! This can't be undone!
    </div>
    Once you delete a course, you won't be able to recover it! All data, including all assignments that were entered, will be deleted from our database permanently!
  </div>
</div>

<%= link_to 'Delete Course', @course, method: :delete, data: { confirm: 'Are you sure you want to delete this course? This action cannot be undone.' }, class: 'ui button red' %>