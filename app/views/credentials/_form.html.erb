<%= form_for(@credential, html: {class: 'ui form'}) do |f| %>
  <% if @credential.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@credential.errors.count, "error") %> prohibited this credential from being saved:</h2>

      <ul>
      <% @credential.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <div class="two fields">
      <div class="field">
        <%= f.label :username %>
        <%= f.text_field :username, placeholder: 'Your iNow Username' %>
      </div>
      <div class="field">
        <%= f.label :password %>
        <%= f.password_field :password, placeholder: 'Your iNow Password' %>
      </div>
    </div>
  </div>
  <div class="actions">
    <%= f.submit class: 'ui button green' %>
  </div>
<% end %>
